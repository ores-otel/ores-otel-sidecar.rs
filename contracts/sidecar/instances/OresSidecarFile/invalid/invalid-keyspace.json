{
  "schema": "ores.sidecar/config/v1",
  "sidecars": [
    {
      "id": "invalid-keyspace",
      "runtime_updates": {
        "provider": "ores-redis-lru-cache",
        "mode": "receive-only",
        "poll_seconds": 180,
        "namespace": "invalid/keyspace",
        "cache": "runtime-env"
      }
    }
  ]
}
