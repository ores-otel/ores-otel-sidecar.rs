{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "ores.sidecar.config.schema.json",
  "$defs": {
    "RuntimeUpdateProvider": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$id": "RuntimeUpdateProvider",
      "type": "string",
      "enum": ["disabled", "ores-redis-lru-cache"]
    },
    "RuntimeUpdateMode": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$id": "RuntimeUpdateMode",
      "type": "string",
      "enum": ["disabled", "receive-only"]
    },
    "RuntimeUpdatePolicy": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$id": "RuntimeUpdatePolicy",
      "type": "object",
      "properties": {
        "provider": { "$ref": "RuntimeUpdateProvider" },
        "mode": { "$ref": "RuntimeUpdateMode" },
        "poll_seconds": { "type": "integer", "minimum": 60, "maximum": 180 },
        "namespace": { "type": "string", "minLength": 1, "maxLength": 64, "pattern": "^[A-Za-z0-9._-]+$" },
        "cache": { "type": "string", "minLength": 1, "maxLength": 64, "pattern": "^[A-Za-z0-9._-]+$" }
      },
      "required": ["provider", "mode", "poll_seconds", "namespace", "cache"],
      "unevaluatedProperties": false
    },
    "RuntimeValueDefinition": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$id": "RuntimeValueDefinition",
      "type": "object",
      "properties": {
        "key": { "type": "string", "minLength": 1, "maxLength": 128, "pattern": "^[A-Z_][A-Z0-9_]*$" },
        "value": { "type": "string", "maxLength": 65536 }
      },
      "required": ["key", "value"],
      "unevaluatedProperties": false
    },
    "SidecarDefinition": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$id": "SidecarDefinition",
      "type": "object",
      "properties": {
        "id": { "type": "string", "minLength": 1, "maxLength": 96, "pattern": "^[A-Za-z0-9._-]+$" },
        "path": { "type": "string", "minLength": 1, "maxLength": 256 },
        "startup_config": { "type": "string", "minLength": 1, "maxLength": 256 },
        "runtime_updates": { "$ref": "RuntimeUpdatePolicy" },
        "runtime_mutable": { "type": "array", "maxItems": 128, "items": { "type": "string" } },
        "values": { "type": "array", "maxItems": 128, "items": { "$ref": "RuntimeValueDefinition" } }
      },
      "required": ["id", "runtime_updates"],
      "unevaluatedProperties": false
    },
    "OresSidecarFile": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$id": "OresSidecarFile",
      "type": "object",
      "properties": {
        "schema": { "type": "string", "const": "ores.sidecar/config/v1" },
        "sidecars": { "type": "array", "minItems": 1, "maxItems": 64, "items": { "$ref": "SidecarDefinition" } }
      },
      "required": ["schema", "sidecars"],
      "unevaluatedProperties": false
    }
  }
}
