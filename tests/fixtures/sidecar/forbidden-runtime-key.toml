schema = "ores.sidecar/config/v1"

[[sidecars]]
id = "secret-sidecar"
runtime_mutable = ["GITHUB_TOKEN"]
[sidecars.runtime_updates]
provider = "ores-redis-lru-cache"
mode = "receive-only"
poll_seconds = 180
namespace = "secret/runtime"
